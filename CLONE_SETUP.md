# 🔄 คู่มือติดตั้งบนเครื่องใหม่

คู่มือสำหรับการ clone และติดตั้งโปรเจค Work Management System บนเครื่องใหม่

---

## ✅ ข้อกำหนดเบื้องต้น

ก่อนเริ่ม ต้องมีสิ่งเหล่านี้ติดตั้งแล้ว:

- ✅ Node.js v14+ ([ดาวน์โหลด](https://nodejs.org/))
- ✅ Git ([ดาวน์โหลด](https://git-scm.com/))
- ✅ SQL Server 2016+ ([ดาวน์โหลด](https://www.microsoft.com/sql-server/sql-server-downloads))
- ✅ SSMS หรือ Azure Data Studio

---

## 🚀 ขั้นตอนการติดตั้ง (10 นาที)

### 1️⃣ Clone โปรเจค

```bash
# เปิด Terminal/PowerShell
cd Desktop  # หรือโฟลเดอร์ที่ต้องการ

# Clone repository
git clone https://github.com/funnyprom/work-management-system.git

# เข้าไปในโฟลเดอร์
cd work-management-system
```

---

### 2️⃣ ติดตั้ง Dependencies

```bash
# ติดตั้ง Frontend dependencies
npm install

# ติดตั้ง Backend dependencies
cd server
npm install
cd ..
```

---

### 3️⃣ สร้างไฟล์ .env (สำคัญมาก! ⚠️)

**ไฟล์เหล่านี้ไม่มีใน GitHub เพราะมี password**

#### ไฟล์ที่ 1: `server/.env`

สร้างไฟล์ในโฟลเดอร์ `server/` ชื่อ `.env` และใส่:

```env
# SQL Server Configuration
DB_SERVER=localhost\SQLEXPRESS
DB_PORT=1433
DB_USER=sa
DB_PASSWORD=your_password_here
DB_DATABASE=WorkManagementDB
DB_ENCRYPT=false
DB_TRUST_SERVER_CERTIFICATE=true

# Server Configuration
PORT=3001
NODE_ENV=development
```

**⚠️ แก้ไขค่าเหล่านี้ให้ตรงกับเครื่องของคุณ:**
- `DB_SERVER`: ชื่อ SQL Server instance ของคุณ
- `DB_USER`: Username ของ SQL Server
- `DB_PASSWORD`: Password ของ SQL Server

**วิธีหาชื่อ SQL Server:**
```powershell
# รันใน PowerShell
Get-Service | Where-Object {$_.Name -like "*SQL*"}
```

---

#### ไฟล์ที่ 2: `.env` (ในโฟลเดอร์ root)

สร้างไฟล์ในโฟลเดอร์หลัก (root) ชื่อ `.env` และใส่:

```env
# API Configuration
VITE_API_URL=http://localhost:3001/api
```

---

### 4️⃣ ตั้งค่า Database

คุณมี **2 ทางเลือก**:

#### 🔸 ตัวเลือก A: สร้าง Database ใหม่บนเครื่องนี้

**ขั้นตอนที่ 1: เปิด SQL Server Management Studio (SSMS)**

**ขั้นตอนที่ 2: Connect to Server**
- Server name: `localhost\SQLEXPRESS` (หรือตามที่คุณตั้งไว้)
- Authentication: SQL Server Authentication
- Login: `sa`
- Password: (รหัสผ่านของคุณ)

**ขั้นตอนที่ 3: รัน Script สร้าง Database**
- File > Open > File...
- เลือกไฟล์: `database/schema.sql`
- กด **F5** หรือคลิก Execute

**ขั้นตอนที่ 4: ตรวจสอบว่าสร้างสำเร็จ**

```sql
USE WorkManagementDB;
SELECT name FROM sys.tables;
```

ควรเห็น:
- Users
- Departments
- Tasks
- PurchaseRequests
- PurchaseRequestItems

---

#### 🔸 ตัวเลือก B: เชื่อมต่อกับ Database บนเครื่องอื่น

**เหมาะสำหรับ:** ทำงานเป็นทีม, ต้องการข้อมูลตรงกัน

**ในไฟล์ `server/.env` แก้เป็น:**

```env
DB_SERVER=IP_ของเครื่องที่มี_Database\SQLEXPRESS
# ตัวอย่าง: DB_SERVER=192.168.1.100\SQLEXPRESS
```

**เครื่องที่มี Database ต้อง:**
1. เปิด SQL Server ให้รับ remote connection
2. เปิด Firewall port 1433
3. SQL Server Browser service ต้อง running

---

### 5️⃣ ทดสอบการเชื่อมต่อ Database

```bash
cd server

# Windows PowerShell:
node -e "require('dotenv').config(); const {getConnection} = require('./config/database'); getConnection().then(() => console.log('✅ Connected!')).catch(err => console.error('❌ Error:', err));"
```

**ถ้าสำเร็จจะเห็น:**
```
✅ Connected to SQL Server successfully
✅ Connected!
```

---

### 6️⃣ รันโปรเจค

**Terminal 1: Backend**
```bash
cd server
npm run dev
```

ควรเห็น:
```
✅ Connected to SQL Server successfully
🚀 Server is running on port 3001
📍 API URL: http://localhost:3001
🏥 Health check: http://localhost:3001/api/health
```

**Terminal 2: Frontend (เปิด Terminal ใหม่)**
```bash
npm run dev
```

ควรเห็น:
```
  VITE v4.x.x  ready in xxx ms
  ➜  Local:   http://localhost:5173/
```

---

### 7️⃣ เปิดเบราว์เซอร์

เปิด: **http://localhost:5173**

ถ้าทุกอย่างถูกต้อง คุณจะเห็นหน้า Dashboard โดย:
- ✅ **ไม่มีข้อความแจ้งเตือนสีแดง**
- ✅ สามารถเพิ่มงานและ PR ได้
- ✅ ข้อมูลถูกบันทึกใน SQL Server

---

## 🧪 การทดสอบว่าทำงานถูกต้อง

### Test 1: ตรวจสอบ Backend API

เปิดเบราว์เซอร์ไปที่: `http://localhost:3001/api/health`

ควรเห็น:
```json
{
  "status": "OK",
  "message": "Work Management API is running"
}
```

### Test 2: เพิ่มงานทดสอบ

1. ไปที่ "ติดตามงาน"
2. คลิก "เพิ่มงานใหม่"
3. กรอกข้อมูลและบันทึก
4. ตรวจสอบใน SQL Server:

```sql
USE WorkManagementDB;
SELECT * FROM Tasks;
```

ควรเห็นงานที่เพิ่ม!

---

## 🔄 การทำงานกับ Git

### ดึงโค้ดล่าสุดจาก GitHub

```bash
# ดึงการเปลี่ยนแปลงล่าสุด
git pull origin main

# ถ้ามีการเพิ่ม dependencies ใหม่
npm install
cd server && npm install && cd ..
```

### Push การเปลี่ยนแปลงของคุณ

```bash
# ดูสถานะไฟล์ที่เปลี่ยน
git status

# เพิ่มไฟล์ที่เปลี่ยนแปลง
git add .

# Commit
git commit -m "คำอธิบายการเปลี่ยนแปลง"

# Push ขึ้น GitHub
git push origin main
```

---

## ⚠️ ข้อควรระวัง

### 1. ไฟล์ .env ไม่มีใน GitHub

- ❌ **ไม่ควร push** ไฟล์ `.env` ขึ้น GitHub (มี password)
- ✅ ไฟล์ `.gitignore` จะช่วยป้องกันไม่ให้ push ไปโดยไม่ตั้งใจ
- ✅ ต้องสร้างไฟล์ `.env` ใหม่ทุกครั้งที่ clone

### 2. Database Schema Changes

ถ้าเครื่องอื่นแก้ไข database schema:

1. Pull โค้ดล่าสุด
2. ดูไฟล์ `database/schema.sql` หรือ migration scripts
3. รันคำสั่ง SQL ที่เปลี่ยนแปลงบนเครื่องของคุณ

### 3. Merge Conflicts

ถ้าหลายคนแก้ไขไฟล์เดียวกัน:

```bash
# Pull ก่อน push เสมอ
git pull origin main

# ถ้ามี conflict
# 1. เปิดไฟล์ที่ conflict
# 2. แก้ไข code ให้ถูกต้อง
# 3. ลบ markers (<<<<<<, ======, >>>>>>)
# 4. Add และ commit
git add .
git commit -m "แก้ conflict"
git push origin main
```

---

## 🐛 แก้ปัญหาที่พบบ่อย

### ❌ ปัญหา: Cannot connect to SQL Server

**ตรวจสอบ:**
1. SQL Server กำลังทำงานอยู่หรือไม่
   ```powershell
   Get-Service | Where-Object {$_.Name -like "*SQL*"}
   ```
2. ค่าใน `server/.env` ถูกต้องหรือไม่
3. TCP/IP enabled หรือไม่ (ใน SQL Server Configuration Manager)

### ❌ ปัญหา: Port 3001 already in use

**วิธีแก้:**
1. ปิด process ที่ใช้ port 3001
   ```powershell
   netstat -ano | findstr :3001
   taskkill /PID [PID] /F
   ```
2. หรือเปลี่ยน port ใน `server/.env`:
   ```env
   PORT=3002
   ```
   และอัพเดท `.env` ในโฟลเดอร์หลัก:
   ```env
   VITE_API_URL=http://localhost:3002/api
   ```

### ❌ ปัญหา: npm install failed

**วิธีแก้:**
```bash
# ลบ node_modules และลองใหม่
rm -rf node_modules package-lock.json
npm install

# สำหรับ Windows PowerShell:
Remove-Item node_modules -Recurse -Force
Remove-Item package-lock.json
npm install
```

### ❌ ปัญหา: Frontend แสดงข้อความแจ้งเตือนสีแดง

**ข้อความ:** "ไม่สามารถโหลดข้อมูลได้ กำลังใช้ข้อมูลจาก Local Storage"

**สาเหตุ:** Backend API ไม่ทำงาน

**วิธีแก้:**
1. ตรวจสอบว่า Backend กำลังรันอยู่ (Terminal ที่รัน `npm run dev` ในโฟลเดอร์ server)
2. ทดสอบ API: เปิดเบราว์เซอร์ไปที่ `http://localhost:3001/api/health`
3. ดู error ใน Terminal ที่รัน Backend

---

## 📞 ต้องการความช่วยเหลือ?

- 📖 อ่าน [README.md](README.md) - เอกสารหลัก
- 📚 อ่าน [SETUP_GUIDE.md](SETUP_GUIDE.md) - คู่มือติดตั้งละเอียด
- ⚡ อ่าน [QUICK_START.md](QUICK_START.md) - เริ่มต้นอย่างรวดเร็ว
- 🔧 อ่าน [server/README.md](server/README.md) - เอกสาร Backend API

---

## ✅ Checklist สำเร็จ

เมื่อทำทุกขั้นตอนแล้ว คุณควรมี:

- ✅ โปรเจคถูก clone มาจาก GitHub
- ✅ Dependencies ติดตั้งครบ (frontend + backend)
- ✅ ไฟล์ `.env` ทั้ง 2 ไฟล์ถูกสร้างและตั้งค่าแล้ว
- ✅ Database `WorkManagementDB` ถูกสร้างพร้อม tables
- ✅ Backend API ทำงานที่ port 3001
- ✅ Frontend ทำงานที่ port 5173
- ✅ เปิดเบราว์เซอร์แล้วไม่มี error
- ✅ สามารถเพิ่ม/แก้ไข/ลบ งานและ PR ได้
- ✅ ข้อมูลถูกบันทึกใน SQL Server

---

**ยินดีต้อนรับสู่ทีม! 🎉 Happy Coding! 🚀**

